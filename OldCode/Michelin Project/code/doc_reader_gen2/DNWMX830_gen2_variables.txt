Here is a summary of the provided COBOL code:

**Program ID:** DNWMX830

**Purpose:** This program is used for shipment confirmation and invoicing.

**Modules:**

1. Initialization
2. WICS Setup
3. Shipment Confirmation
4. Invoicing
5. Error Handling
6. Audit
7. Internal JCL Submission
8. External JCL Submission

**Functions:**

1. Bind database records
2. Process shipment confirmation transaction set
3. Process invoicing
4. Handle errors and exceptions
5. Perform audit and logging
6. Submit internal JCL
7. Submit external JCL
8. Rollback database updates

**Data Structures:**

1. WICSRECV-CONTROL-RECORD
2. WICSTRAN-REQUEST-BLOCK
3. WSC-DATA-RECORD
4. WICSEBOL-TABLE
5. WICS-LOADS
6. TABLE-HDR
7. TABLE-DETAIL
8. FREEFORM-ITEMS

**Variables:**

1. WS-ITEM-CNT
2. WS-VAS-INDEX
3. WS-QTY-SHIPPED
4. WS-OLD-DATE
5. WS-TEST-DATE
6. WS-TEST-TIME
7. WS-DATE-KNOWN
8. WS-DATE-UNKNOWN
9. WS-DAY-COUNT
10. WS-CHECK-BYTE
11. WS-RETURN-CODE
12. WS-VARIABLES
13. WS-WORK-ELEMENTS
14. WS-SOLOMON-PLANT
15. WS-BILL-TO
16. WS-SHIP-TO
17. WS-IFNUM-SWAP-FLAG
18. WS-PUSH-LOAD-FLAG
19. WS-INVOICE-TARGET-SYS
20. WS-SIMULATE-TARGET-SYS
21. WS-ALIAS-FOUND
22. WS-LOGMSG-ON
23. WS-LOGMSG-STATUS
24. WS-MESSAGE-RECORD
25. WS-GET-FREIGHT-FLAG
26. WS-IFNUM-SWAP-YES
27. WS-IFNUM-SWAP-NO
28. WS-PUSH-LOAD-YES
29. WS-PUSH-LOAD-NO
30. WS-INVOICE-TARGET-SYS
31. WS-SIMULATE-TARGET-SYS
32. WS-ALIAS-FOUND
33. WS-LOGMSG-ON
34. WS-LOGMSG-STATUS
35. WS-MESSAGE-RECORD
36. WS-GET-FREIGHT-FLAG
37. WS-ALIAS-FOUND
38. WS-LOGMSG-ON
39. WS-LOGMSG-STATUS
40. WS-MESSAGE-RECORD
41. WS-GET-FREIGHT-FLAG
42. WS-ALIAS-FOUND
43. WS-LOGMSG-ON
44. WS-LOGMSG-STATUS
45. WS-MESSAGE-RECORD
46. WS-GET-FREIGHT-FLAG
47. WS-ALIAS-FOUND
48. WS-LOGMSG-ON
49. WS-LOGMSG-STATUS
50. WS-MESSAGE-RECORD
51. WS-GET-FREIGHT-FLAG
52. WS-ALIAS-FOUND
53. WS-LOGMSG-ON
54. WS-LOGMSG-STATUS
55. WS-MESSAGE-RECORD
56. WS-GET-FREIGHT-FLAG
57. WS-ALIAS-FOUND
58. WS-LOGMSG-ON
59. WS-LOGMSG-STATUS
60. WS-MESSAGE-RECORD
61. WS-GET-FREIGHT-FLAG
62. WS-ALIAS-FOUND
63. WS-LOGMSG-ON
64. WS-LOGMSG-STATUS
65. WS-MESSAGE-RECORD
66. WS-GET-FREIGHT-FLAG
67. WS-ALIAS-FOUND
68. WS-LOGMSG-ON
69. WS-LOGMSG-STATUS
70. WS-MESSAGE-RECORD
71. WS-GET-FREIGHT-FLAG
72. WS-ALIAS-FOUND
73. WS-LOGMSG-ON
74. WS-LOGMSG-STATUS
75. WS-MESSAGE-RECORD
76. WS-GET-FREIGHT-FLAG
77. WS-ALIAS-FOUND
78. WS-LOGMSG-ON
79. WS-LOGMSG-STATUS
80. WS-MESSAGE-RECORD
81. WS-GET-FREIGHT-FLAG
82. WS-ALIAS-FOUND
83. WS-LOGMSG-ON
